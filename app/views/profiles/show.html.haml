.container
  .form-title プロフィール
  .form-container
    = render "profile", profile: @profile

    -# プロフィール編集画面へのリンク
    .btn-container
      = link_to "編集", edit_profile_path(@profile), class: "btn-secondary"

- if turbo_frame_request?
  = render "profile_modal", profile: @profile
